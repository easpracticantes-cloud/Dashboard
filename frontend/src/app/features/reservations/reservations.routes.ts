import { Routes } from '@angular/router';
import { ReservationsComponent } from './reservations.component';

export const RESERVATIONS_ROUTES: Routes = [{ path: '', component: ReservationsComponent }];
