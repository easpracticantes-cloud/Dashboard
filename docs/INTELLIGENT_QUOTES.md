# Cotizaciones en SIG (Ave)

La cotización es una **hoja HTML/CSS** fiel a la plantilla Escuela Aves (logo, ave, tipografías, colores, footer).

1. Ave + Claude arman el borrador con catálogo.
2. Se abre la hoja editable (campos inline con recálculo).
3. Guardar / Generar PDF (captura de la hoja renderizada).

WhatsApp / Registro no alimentan este flujo.
