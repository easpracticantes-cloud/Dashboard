package com.escuelaaves.sig.domain.model;

/**
 * Roles del CRM empresarial SIG.
 * Se mantienen roles legacy y se añaden ASESOR / SUPERVISOR
 * para el modelo comercial solicitado.
 */
public enum RoleName {
    ADMINISTRADOR,
    SUPERVISOR,
    ASESOR,
    GERENCIA,
    COMERCIAL,
    CONTABILIDAD,
    OPERACIONES
}
